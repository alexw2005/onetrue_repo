version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - pnpm i --frozen-lockfile
    build:
      commands:
        - pnpm --filter @onetrue/app-marketplace build
  artifacts:
    baseDirectory: apps/marketplace/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - apps/**/node_modules/**/*
      - packages/**/node_modules/**/*
